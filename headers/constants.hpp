#pragma once

#include <SFML/Graphics.hpp>

namespace GEngine{
    class Constants{
    public:
        int SCREEN_WIDTH;
        int SCREEN_HEIGHT;
    
        const sf::Color RED = sf::Color(255,20,20);
};
}